const welcomeIntents = require("./welcome");
const helpIntents = require("./help");
const fallbackIntents = require("./fallback");
const yesIntents = require("./yes");
const noIntents = require("./no");

module.exports = {
  welcomeIntents,
  helpIntents,
  fallbackIntents,
  yesIntents,
  noIntents,
};
